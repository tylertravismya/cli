app:
    - techStackId:       Django
      modelId:           4                                          # id or file; ignored when provided via cmd line arg
      appOptionsFile:    ./samples/options/Django.options.json      # ignored when provided via cmd line arg
      gitParams:
         entry:          git_test_Django
         file:           ./samples/git/github.yml                   # ignored when provided via cmd line arg
      saveParams:
         name:           Django v0.0.1
         description:    Django Test Application Generation
